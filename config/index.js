'use strict'

const path = require('path')

module.exports = {
  dev: {

  },

  build: {


    // paths
    assetsRoot: path.resolve(__dirname, '../dist'),
    assetsSubDirectory: 'static',
    assetsPublicPath: '/'
  }
}

  // "node-sass": "^4.9.2",